package snowflakex

import (
	"testing"
)

func TestGetIdArr(t *testing.T) {
	//set := []int64{}
	//arr, err := GetIdArr(10000)
	//for _, i := range arr {
	//	println(i)
	//	for _, j := range set {
	//		if i != j {
	//			set = append(set, i)
	//		}
	//	}
	//}
	//fmt.Println(len(arr), err)
	//fmt.Println(len(set), err)
}
